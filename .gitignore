# Xcode
DerivedData/
*.xcuserstate
xcuserdata/

# Swift Package Manager
.build/
.swiftpm/xcode/package.xcworkspace/

# macOS
.DS_Store
*.swp
*~

# Build artifacts
*.o
*.d
*.pcm
*.swiftmodule
*.swiftdeps
*.swiftdeps~
*.priors

# IDE / editor
.idea/

# Local build & release artifacts (never tracked)
build/
release/
*.dmg
*.xcresult
*.profraw
default.profraw

# Xcode user data (scheme management, breakpoints)
*.xcuserdatad

# Accidental capture files
amp

# Signing (contains Team ID — use the .template)
Signing.xcconfig

# QA transient run artifacts (reports/latest.html + run.json are published)
qa/runs/

# Perf-measurement derived data (scripts/perf runs)
.perfbuild/
