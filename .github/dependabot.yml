# Keeps the SHA-pinned actions fresh (Dependabot updates the pin and carries the
# version comment). SPM packages are all internal — no pip/swift ecosystems here.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns: ["*"]
